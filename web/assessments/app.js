const buttons=[...document.querySelectorAll("[data-filter]")];const cards=[...document.querySelectorAll(".assessment")];for(const button of buttons){button.addEventListener("click",()=>{for(const item of buttons)item.classList.remove("active");button.classList.add("active");for(const card of cards)card.classList.toggle("hidden",button.dataset.filter!=="all"&&card.dataset.type!==button.dataset.filter);});}document.querySelector("#printBtn").addEventListener("click",()=>window.print());
