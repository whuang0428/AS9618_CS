function explanation(lesson, targetId, kind, title, paragraphs) {
  return Object.freeze({ lesson, targetId, kind, title, paragraphs: Object.freeze(paragraphs) });
}

export const explanations = Object.freeze([
  explanation("016", "purpose", "mechanism", "Why sharing becomes useful only when devices can exchange requests", [
    "A network creates value by giving devices a controlled path for exchanging data. When a computer sends a print job, for example, it does not somehow use the printer from a distance: it packages the request, sends it across a link and identifies the shared printer as the destination. The same mechanism lets users open centrally stored files or send messages. Sharing reduces duplication because one managed resource can answer requests from many authorised devices. Central management works for the same reason: accounts, permissions, updates and backups can be applied at a server or management service rather than configured independently on every machine. This convenience also creates dependence. If the shared service or connecting network fails, many users may lose access at once. A strong explanation therefore links the benefit to the shared communication path and also recognises the new point of failure.",
  ]),
  explanation("016", "lanwan", "comparison", "Why area and ownership change the way a network is managed", [
    "LAN and WAN are not labels for fast and slow networks. They describe different scales and management arrangements. A LAN normally covers a limited site and is controlled by one organisation, so that organisation can choose the switches, cabling, wireless access points and security rules. Shorter local links and direct control often make faults easier to locate and upgrades easier to schedule. A WAN connects networks across a much larger area, so traffic may cross infrastructure owned by telecommunications providers. That adds distance, routing decisions and dependence on external services, which can increase latency, cost and troubleshooting complexity. A school network does not become a WAN merely because it uses Wi-Fi, and a WAN is not automatically the public internet. The deciding questions are what geographical area is connected and who controls the infrastructure between the connected sites.",
  ]),
  explanation("016", "topologies", "tradeoff", "Why physical connection patterns create different costs and failure effects", [
    "A topology changes network behaviour because it determines the path that data and failures can follow. In a bus, every device depends on one backbone, so little cabling is needed but damage or congestion on that shared path affects the whole network. In a star, each device has its own link to a central switch. One cable failure normally isolates one device, yet switch failure removes the common forwarding point. In a full mesh, every pair of nodes has a direct link, so another path remains when one link fails. That resilience is expensive because the number of links and ports rises rapidly as nodes are added. A hybrid combines topology types, such as connected star segments, to balance local simplicity with wider resilience. No topology is always best: the correct choice follows from required availability, expansion, cost and the consequence of each possible failure.",
  ]),

  explanation("030", "primary", "mechanism", "Why primary storage must stay close to current processing", [
    "The processor can only work efficiently when instructions and current data are available through fast, directly addressable storage. Primary storage provides that working area. Programs are copied from secondary storage into RAM because an SSD or hard disk is designed for persistent capacity, not for supplying every instruction at processor speed. While a program runs, RAM holds its active instructions, data and intermediate results so the CPU can fetch them repeatedly. Registers and cache are even closer and faster, but they are much smaller, so they cannot replace RAM for the whole running program. This layered design balances speed, capacity and cost. Calling primary storage a filing cabinet misses the mechanism: it is the live workspace that feeds current processing. When it is too small, the system must move data between RAM and slower storage more often, which is why performance can fall even though no computation has changed.",
  ]),
  explanation("030", "ram-rom", "comparison", "Why RAM is temporary while ROM remains stable", [
    "RAM and ROM serve different jobs because the system needs both changeable working space and stable startup instructions. RAM is read and written continually as applications create variables, load files and produce intermediate results. Its contents are volatile, so removing power clears that temporary electrical state. This is acceptable because the working data can be recreated or loaded again. ROM stores instructions that should remain available when the machine is powered off, such as firmware needed to begin the startup process. Its non-volatile design preserves those instructions without power, while normal operation does not require them to be rewritten constantly. The distinction is therefore not simply that one can be read and the other cannot: modern ROM technologies may be updated under controlled conditions. The important causal difference is how often the contents must change and whether they must survive loss of power.",
  ]),
  explanation("030", "cache-vm", "comparison", "Why cache accelerates access while virtual memory usually slows it", [
    "Cache and virtual memory both affect the apparent amount of useful memory, but they solve opposite problems. Cache keeps copies of recently or frequently used instructions and data in very fast storage close to the CPU. Programs often reuse nearby values, so a cache hit avoids a slower trip to RAM and reduces the time the processor waits. Virtual memory is used when RAM cannot hold all active pages. The operating system moves less active pages to secondary storage and brings them back when required. This prevents an immediate out-of-memory failure, but secondary storage is much slower than RAM, so repeated page movement can make the system feel unresponsive. More cache does not permanently store more user data, and virtual memory does not make a computer faster. Cache exploits locality to reduce access time; virtual memory trades speed for the ability to continue running a larger working set.",
  ]),

  explanation("041", "architecture", "mechanism", "Why a CPU needs specialised parts instead of one general component", [
    "CPU components cooperate because processing an instruction requires several different jobs. Registers hold the small values and instructions needed immediately. The control unit interprets the current instruction and sends control signals that coordinate transfers and operations. The ALU performs arithmetic and logical work, while buses carry addresses, data and control signals between the CPU, memory and other components. A clock provides repeated timing signals so these state changes occur in an organised sequence rather than colliding unpredictably. Specialisation makes the movement of data explicit: an address is placed where memory can receive it, returned data is held before use, and the selected operation is applied to the correct operands. Saying that the CPU simply “processes data” hides this mechanism. The processor works because each component performs a limited role and the control unit coordinates those roles for every instruction.",
  ]),
  explanation("041", "alu-cu", "comparison", "Why the ALU calculates but the control unit coordinates", [
    "The ALU and control unit are both inside the CPU, but they contribute in different ways. The ALU contains circuits for operations such as addition, subtraction, comparison and Boolean logic. It can produce a result only after it receives operands and an operation to perform. The control unit supplies that coordination. It decodes the instruction in the CIR, identifies the required operation and issues signals that move values between registers, memory and the ALU. This is why the control unit does not itself add two numbers, and the ALU does not decide which instruction should execute next. Their relationship is like selecting and carrying out a command: the control unit determines the sequence and enables the correct pathways; the ALU performs the requested calculation. Confusing the roles produces vague answers because it removes the cause of each component's behaviour.",
  ]),
  explanation("041", "registers", "mechanism", "Why registers, buses and the clock must work as one timed system", [
    "Registers provide named holding places for values that the CPU needs during an instruction. A register is useful because its contents can be accessed and changed quickly, but the value still has to reach the correct destination. Buses provide those routes: the address bus identifies a location, the data bus carries the value, and the control bus carries signals such as read or write. The system clock divides activity into coordinated steps. On a clock event, one component may place a value on a bus while another accepts it, preventing unrelated transfers from being treated as the same operation. A higher clock rate allows more timing cycles per second, but it does not guarantee proportional performance because instruction complexity, memory delay and architecture also matter. The mechanism is therefore timed movement between specialised stores, not a collection of independent components working whenever they choose.",
  ]),

  explanation("053", "concept", "mechanism", "Why applications need an operating system between them and the hardware", [
    "Applications need processor time, memory, files and devices, but allowing every program to control those resources directly would create conflicts and hardware-specific code. The operating system provides managed services between applications and the hardware. An application requests an operation, such as opening a file or sending output to a printer, and the OS checks permissions, selects the correct driver and schedules access to the resource. This common interface means the application does not need separate low-level instructions for every model of storage device or printer. Management also protects one program from another: memory regions can be isolated and device requests can be queued rather than executed simultaneously. The OS is therefore not useful merely because it offers a graphical interface. Its essential role is to coordinate shared resources safely and present predictable services on which applications can depend.",
  ]),
  explanation("053", "process", "process", "Why sharing processor time creates the appearance of simultaneous programs", [
    "A processor core can execute only one instruction stream at an instant, yet users expect several applications to remain responsive. The operating system achieves this by scheduling processes. It gives a process a short period of CPU time, records the process state when that period ends or the process waits for input, and then restores another process so execution can continue from its saved point. Rapid switching creates the appearance that programs run together. Priorities and scheduling rules influence which process runs next, preventing one ordinary task from keeping the processor indefinitely. The benefit comes from controlled switching, but switching also has a cost because saving and restoring state performs no user calculation. Multicore processors can genuinely execute more than one stream at once, yet each core still requires scheduling when runnable processes outnumber available cores.",
  ]),
  explanation("053", "memory", "mechanism", "Why memory allocation and protection are both necessary", [
    "Each running process needs space for its instructions, variables and temporary results. The operating system allocates regions of memory so that programs know where their current data can be stored. It also records which process owns each region and prevents an ordinary process from reading or overwriting another process's memory. Without this protection, one faulty program could corrupt another program or expose private data. When a process ends, the OS marks its regions as available so the same physical memory can be reused. If demand exceeds RAM, memory management may move pages to secondary storage, trading speed for capacity. Allocation alone is not enough: safe reuse requires ownership, address translation and protection rules. This explains why memory management affects both reliability and performance rather than simply reporting how much RAM is installed.",
  ]),
  explanation("053", "file", "mechanism", "Why a file system needs names, metadata and access rules", [
    "Storage devices contain blocks of data, not naturally meaningful documents and folders. The operating system's file system maps human-readable names and directories to the blocks that store each file. Metadata records properties such as size, location, timestamps and permissions, allowing the OS to find the data and decide whether a user may read or change it. When a file grows, the file system allocates additional free blocks and updates its records; when a file is deleted, those blocks can be returned to the free-space pool. This organisation explains why renaming a file usually does not rewrite all its contents and why a damaged file-system index can make intact blocks difficult to locate. File management is therefore more than displaying folders: it maintains the structured relationship between names, ownership, storage locations and reliable retrieval.",
  ]),
  explanation("053", "device", "process", "Why drivers, buffers and queues make slow devices manageable", [
    "Hardware devices expose different commands and operate at very different speeds. A driver translates the operating system's standard request into instructions understood by a particular device. A buffer temporarily holds data when the producer and consumer cannot transfer at the same rate; for example, a program can place print data in memory while the printer handles it more slowly. A queue records the order of outstanding requests so several programs do not attempt to control the same device at once. Together these mechanisms separate the application's progress from the device's physical timing. The buffer is not permanent storage and it does not make the printer itself faster. It prevents the faster component from waiting for every small transfer, while the queue and driver ensure that requests reach the correct device in a controlled form.",
  ]),
  explanation("053", "services", "synthesis", "Why operating-system services form one coordinated control layer", [
    "Security, user interfaces, networking, error handling and system monitoring appear to be separate OS features, but they depend on the same privileged control layer. The operating system knows which user and process made a request, which resource is involved and which policy applies. It can therefore authenticate a user, check authorisation, record an error and present the result through a common interface. Networking services similarly coordinate access to adapters and protocol settings rather than allowing every application unrestricted hardware control. The exact services vary between operating systems, so an exam answer should connect a named service to its mechanism instead of claiming every OS behaves identically. The unifying reason these services belong in the OS is that they require trusted, system-wide information and must arbitrate between multiple programs or users.",
  ]),

  explanation("067", "core", "comparison", "Why security methods cannot substitute for one another", [
    "Security methods solve different questions. Encryption asks who can read captured data; hashing asks whether a value matches or has changed; a digital certificate helps establish whose public key is being presented. These questions can occur in the same connection, which is why the methods are often used together, but combining them does not erase their separate mechanisms. Encrypting a password for transmission does not provide a safe long-term comparison method by itself, and hashing a message does not keep the message secret. A certificate is also not an encrypted copy of a website. Choosing a method begins with the required property: confidentiality, integrity, authentication or non-repudiation. A precise explanation names the threat, describes how the method changes what an attacker can do and acknowledges what the method does not protect against.",
  ]),
  explanation("067", "encryption", "mechanism", "Why ciphertext protects confidentiality but not every security property", [
    "Encryption transforms plaintext into ciphertext using an algorithm and a key. Without the appropriate decryption key, an intercepted ciphertext should not reveal the original data feasibly. This protects confidentiality while data are stored or transmitted because observing the bytes is no longer enough to understand their meaning. The mechanism depends on protecting keys and using a sound algorithm; weak keys or stolen keys remove the advantage. Encryption alone does not prove that the sender is genuine, prevent deletion or guarantee that decrypted data are accurate. Other controls are needed for identity, integrity and availability. The causal chain is therefore specific: a secret or controlled key makes captured data unreadable to unauthorised parties. Statements such as “encryption makes a system secure” are too broad because the system can still fail through compromised endpoints, poor permissions or unavailable services.",
  ]),
  explanation("067", "keys", "mechanism", "Why key ownership determines who can perform each cryptographic action", [
    "A cryptographic algorithm is normally public; secrecy or authority comes from the key. In symmetric encryption, the same secret key encrypts and decrypts, so both parties need a secure way to obtain it. Anyone who gains that key can usually read and create protected messages. In asymmetric cryptography, a public key can be distributed while the related private key remains controlled by its owner. Data encrypted for confidentiality with the public key can be decrypted with the private key, while a digital signature created with the private key can be checked with the public key. The operations are related but have different purposes. A public key is not evidence of identity by itself, because an attacker could publish a replacement key. Certificates and trusted verification connect a public key to the claimed owner.",
  ]),
  explanation("067", "hashing", "mechanism", "Why hashing supports comparison without storing the original value", [
    "A hash function maps an input of any practical length to a fixed-size digest. A small change to the input should produce a very different digest, while computing the original input from the digest should be infeasible. A system can therefore store a password hash and later hash the submitted password; matching digests provide evidence that the same password was entered without keeping the plaintext password. Secure password storage also uses a unique salt and a deliberately expensive password-hashing function so attackers cannot efficiently test large lists of guesses. Hashing is not encryption because there is no decryption key and no intended reversal step. It supports integrity or comparison, not confidentiality: anyone who sees an unhashed document can still read it, and a digest alone does not identify who created the document.",
  ]),
  explanation("067", "certificates", "mechanism", "Why a certificate makes a public key more trustworthy", [
    "A secure connection needs the server's public key, but receiving a key does not prove who owns it. A digital certificate binds a public key to an identified subject and is signed by a certificate authority. The browser checks the authority's signature, the requested hostname, the validity period and other constraints. If those checks succeed and the authority is trusted, the browser has evidence that the key belongs to the named site rather than an interceptor. The certificate does not guarantee that the website is honest or free of malware; it authenticates the key-to-identity binding under the authority's rules. This mechanism blocks a simple substitution attack because an attacker cannot replace the site's key and also produce a valid authority signature for the same hostname without compromising the trust system.",
  ]),
  explanation("067", "https", "process", "Why HTTPS needs both authentication and encrypted transport", [
    "HTTPS uses TLS to establish a protected channel before ordinary HTTP data are exchanged. The server presents a certificate, the client validates the server identity and the two sides establish session keys. Symmetric encryption is then normally used for the data because it is efficient for a long stream, while asymmetric operations support authentication and secure key establishment. Integrity checks allow altered traffic to be detected. These stages explain why HTTPS protects credentials and page contents from simple interception and modification on the network. It does not make every action on the website safe: the authenticated site may still contain insecure application code, collect unnecessary data or be controlled by a legitimate but malicious operator. HTTPS secures the connection to the identified endpoint; it is not a judgement about all content behind that endpoint.",
  ]),
  explanation("067", "compare", "tradeoff", "Why the required security property should choose the method", [
    "A good security choice starts with the failure that must be prevented. If an eavesdropper must not understand captured data, encryption addresses confidentiality. If a receiver must detect changed data, a cryptographic hash or message-authentication mechanism addresses integrity. If a browser must know which organisation controls a public key, a certificate supports authentication. Digital signatures combine hashing with private-key operations to provide origin and integrity evidence. Real systems use layers because one threat can remain after another is controlled. Adding more methods without a threat model can waste effort or create false confidence. The exam technique follows the engineering logic: name the asset and threat, state the method's mechanism, then link that mechanism to the required property. Do not select a method because its name sounds more advanced.",
  ]),

  explanation("083", "purpose", "mechanism", "Why normalisation protects consistency rather than merely making tables smaller", [
    "Normalisation reorganises data so that each independent fact is stored in an appropriate relation and can be referenced through keys. The main goal is consistency, not simply reducing the number of characters stored. If a tutor's email is repeated in every enrolment row, changing one copy while missing another creates contradictory versions of the same fact. Moving tutor details to a Tutor table gives the email one authoritative row, while enrolments store a tutor key. Queries can reconstruct the combined view through that relationship. This design may require joins and can introduce more tables, so it is not automatically faster for every query. Its benefit comes from controlling dependencies: one fact has one intended place to be inserted, updated or deleted, reducing the opportunities for different copies to disagree.",
  ]),
  explanation("083", "redundancy", "mechanism", "Why repeated facts create risk even when every copy starts correctly", [
    "Redundancy becomes harmful when the same real-world fact is stored independently in several rows. At the moment of entry, every copy may agree, so the table appears correct. The risk appears when the fact changes. Each copy must be found and updated, and one missed row creates inconsistency. Repetition also wastes storage and makes validation harder because the database cannot easily identify which copy is authoritative. Not every repeated value is unwanted redundancy: many students may legitimately share the same course code, and each enrolment needs that reference. The problem is repeating attributes that depend on a different entity, such as storing the course title and tutor email in every enrolment. Dependency analysis reveals where the fact belongs; keys then preserve the relationship without copying the full fact.",
  ]),
  explanation("083", "anomalies", "tradeoff", "Why one badly structured table produces three different anomalies", [
    "Insertion, update and deletion anomalies are different symptoms of the same structural problem: unrelated facts have been forced into one row type. An insertion anomaly occurs when a new fact cannot be stored without inventing another fact, such as being unable to add a course until a student enrols. An update anomaly occurs when one real-world change requires several row changes, allowing copies to disagree. A deletion anomaly occurs when removing the last row for one fact accidentally removes the only stored copy of another, such as deleting the last enrolment and losing the course details. Splitting the table by entity and connecting rows with keys separates the lifecycles of those facts. The anomalies disappear because each entity can be inserted, changed or removed without performing an unrelated operation.",
  ]),
  explanation("083", "normal-forms", "process", "Why each normal form removes a different dependency problem", [
    "Normal forms are checkpoints for dependencies, not a ritual of splitting tables. First normal form requires atomic values and a consistent row structure so that each field contains one value that can be addressed. Second normal form matters when a primary key has several attributes: every non-key attribute must depend on the whole key, otherwise facts about only part of the key belong elsewhere. Third normal form removes transitive dependency, where a non-key attribute determines another non-key attribute. At each step, the misplaced attributes move to a relation whose key actually determines them, and foreign keys preserve the connection. A design should not be split without examining dependencies, because unnecessary tables add joins without correcting a real anomaly. The reason for each decomposition is always the same question: what fact determines this attribute?",
  ]),

  explanation("091", "logic", "synthesis", "Why hardware answers improve when components are connected into mechanisms", [
    "Hardware topics become easier to retrieve when they are organised as cause-and-effect systems rather than isolated definitions. An input device converts a physical event into data; storage keeps the data; buses move values and signals; registers hold the values needed immediately; the control unit coordinates transfers; and the ALU performs the selected operation. Logic gates explain how binary conditions can control those operations at circuit level. During the fetch-decode-execute cycle, each named register matters because of the value it holds and the next transfer it enables. This connected model also improves evaluation questions: choosing a component requires matching its physical or logical mechanism to the scenario, not repeating that it is “fast” or “efficient”. When revising, trace one value from input through storage and processing to output, naming why each component is required at that point.",
  ]),

  explanation("105", "bubble", "process", "Why bubble sort needs repeated adjacent comparisons", [
    "Bubble sort compares neighbouring items because a swap can correct a local inversion without losing any data. During one left-to-right pass in ascending order, whenever the left item is larger, the pair is swapped. The largest remaining item therefore moves one position at a time toward the right until it reaches its final position at the end of that pass. One pass cannot usually sort the whole list because smaller misplaced values may need to move left across several later comparisons. Repeating passes shrinks the unsorted region. If a complete pass makes no swaps, every adjacent pair is already ordered, so the algorithm can stop early. The name “bubble” describes this gradual movement of an extreme value; it does not mean the algorithm compares every possible pair directly.",
  ]),
  explanation("105", "insertion", "process", "Why insertion sort maintains a sorted left section", [
    "Insertion sort treats the left part of the list as already sorted. It takes the next item, saves it, and shifts larger items in the sorted section one position to the right until the correct gap appears. The saved item is then inserted into that gap. Shifting is necessary because writing the item immediately would overwrite a value that still has to be kept. After the insertion, the sorted section has grown by one item and remains ordered, which is the invariant that makes the next step valid. The algorithm can perform well on nearly sorted data because an item that is already close to its position requires few shifts. It still may require many comparisons and movements when small items begin near the end. The mechanism is insertion into a maintained ordered prefix, not repeated swapping of arbitrary pairs.",
  ]),
  explanation("105", "compare", "comparison", "Why two correct sorting methods move data differently", [
    "Bubble and insertion sort can produce the same ordered result while following different invariants. Bubble sort uses adjacent comparisons across passes and places an extreme item at the end of the unsorted region. Insertion sort preserves a sorted prefix and places the next item inside it. This difference changes the trace: bubble sort records swaps between neighbours, while insertion sort may save one item and shift several larger values. Both can take a quadratic number of comparisons in difficult cases, so neither is universally efficient for large data sets. On nearly sorted data, insertion sort often performs little movement, and an optimised bubble sort can stop after a pass with no swaps. A comparison is meaningful only when it names the movement, stopping condition or data state that causes the performance difference.",
  ]),
  explanation("105", "pseudocode", "comparison", "Why a trace should follow algorithm state rather than language punctuation", [
    "Cambridge pseudocode expresses the intended algorithm without depending on Java syntax. A trace should therefore follow variable values, comparisons, swaps, loop boundaries and the changing list, not semicolons or type declarations. Java may use zero-based array indexes and library methods, while a pseudocode question may specify different bounds and require the movement to be written explicitly. Copying a remembered Java loop can introduce an off-by-one error even when the sorting idea is correct. Begin with the stated lower and upper bounds, identify which part of the list is already in its final or sorted state, and update that state after each pass or insertion. The language notation is a representation; the algorithm is the sequence of state changes. A correct trace remains correct when rewritten in another language because those state changes are preserved.",
  ]),

  explanation("122", "concept", "mechanism", "Why an ADT is defined by permitted behaviour", [
    "An abstract data type describes what operations are available and what behaviour those operations guarantee. It deliberately separates that contract from the storage used to implement it. A stack may use an array or linked structure, but users of the stack still push and pop at the same end. This separation allows the implementation to change without changing code that relies on the ADT operations. It also prevents accidental access patterns that would break the intended behaviour. Calling an array a stack merely because it stores stack items is therefore incomplete; the code must enforce stack operations and state rules. Abstraction is useful because programmers can reason about a small set of guarantees instead of every memory detail, while implementers remain free to choose a representation that fits capacity and performance requirements.",
  ]),
  explanation("122", "stack", "process", "Why one accessible end produces LIFO behaviour", [
    "A stack adds and removes items at the same end, called the top. When a new item is pushed, it becomes the only item that pop may remove next. Older items remain underneath it until every newer item above them has been removed. This restriction produces last-in, first-out behaviour without requiring timestamps or a search for the newest value. A top pointer or index records the current accessible position, so push changes it in one direction and pop reverses that change. The mechanism suits nested tasks such as function calls or undo operations because the most recently started action must normally finish first. If code removes an item from the bottom, it has stopped behaving as a stack even if the data are still stored in the same array.",
  ]),
  explanation("122", "queue", "process", "Why separate front and rear positions produce FIFO behaviour", [
    "A queue adds new items at the rear and removes existing items from the front. Because new arrivals cannot jump ahead of items already waiting, removal follows first-in, first-out order. Front and rear pointers let these operations occur without searching for the oldest item. In an array implementation, moving every item after a dequeue would be wasteful, so a circular queue often lets the pointers wrap to the beginning and reuse free positions. The logical queue order is then determined by the pointers, not by the visual left-to-right order of the array. This mechanism suits print jobs and service requests where earlier arrivals should normally be handled first. Priority queues follow different removal rules and should not be used as evidence that an ordinary queue is not FIFO.",
  ]),
  explanation("122", "operations", "comparison", "Why operation names protect the meaning of the structure", [
    "ADT operation names communicate both an action and the rule under which it occurs. Push and pop imply access at the top of a stack; enqueue and dequeue imply addition at the rear and removal at the front of a queue. Replacing these with a vague word such as add or remove hides the position and makes it impossible to verify LIFO or FIFO behaviour. Peek returns the next accessible item without changing the structure, which is different from pop or dequeue. Clear, isEmpty and isFull describe state rather than moving data. In an implementation, these operations may use ordinary array assignments, but callers should use the ADT interface so they cannot bypass its restrictions. The vocabulary matters because it preserves the behavioural contract even when the internal representation changes.",
  ]),
  explanation("122", "errors", "tradeoff", "Why overflow and underflow must be checked before changing state", [
    "Underflow occurs when a removal operation is requested from an empty structure; overflow occurs when an insertion is requested but a fixed-capacity implementation has no free space. The check must happen before pointers or stored values are changed. Otherwise a pop might move the top pointer outside the valid range, or a push might overwrite unrelated memory or an existing item. A dynamic implementation may grow rather than report overflow, but it still needs to handle allocation failure and defined capacity limits. The ADT's public behaviour should specify what happens, such as returning an error value or rejecting the operation. These errors are not properties of the abstract ordering rule itself: they arise from current state and implementation limits. Correct code separates the state check from the successful operation so a failed request leaves the structure unchanged.",
  ]),
  explanation("122", "implementation", "mechanism", "Why array storage needs pointers to enforce ADT behaviour", [
    "An array provides indexed storage but does not know which index is the top of a stack or the front of a queue. Additional state variables give those positions meaning. A stack top index identifies the most recently occupied position. A queue normally needs front and rear positions, and often an item count, especially when the array is circular. The operations update these variables in a controlled order and then read or write the corresponding element. Directly accessing an arbitrary array index would bypass the ADT rule even though the underlying data remain valid. Fixed arrays make capacity checks necessary, while linked implementations replace indexes with references. The representation changes the code and performance characteristics, but the observable push, pop, enqueue and dequeue behaviour must remain the same.",
  ]),
  explanation("122", "pseudocode", "comparison", "Why exam pseudocode should expose the state change clearly", [
    "Pseudocode for an ADT operation should make its precondition and state change visible. Before pushing to a fixed stack, it checks capacity; before popping, it checks whether the stack is empty. It then updates the top position and reads or writes the array in the order required by the chosen convention. Queue code similarly shows which pointer identifies the removal position and how wrap-around is handled. Java collection methods may hide these details, so copying a library call does not demonstrate the requested algorithm. Conversely, if a question asks only for the use of an ADT, the named operations may be enough and reimplementing the structure wastes time. The required abstraction level comes from the question: trace the interface when behaviour is tested, and expose pointers and checks when implementation is tested.",
  ]),

  explanation("142", "purpose", "mechanism", "Why a lifecycle reduces uncertainty instead of merely adding documents", [
    "Software projects fail when important questions are discovered after expensive decisions have already been made. A development lifecycle gives those questions an organised place: requirements clarify the problem, design decides a solution structure, implementation builds it, testing checks behaviour and evaluation compares the result with the original need. The stages create evidence that can be reviewed before the project commits further time and money. Documentation is useful only when it records decisions, constraints and results that later work depends on; producing documents without using them does not control risk. Different lifecycle models arrange feedback differently, but all try to make uncertainty visible and manageable. The benefit therefore comes from earlier detection, traceable decisions and controlled change, not from following stage names as a ceremonial checklist.",
  ]),
  explanation("142", "stages", "process", "Why each lifecycle stage must produce information for the next", [
    "Lifecycle stages form a chain of decisions. Analysis turns stakeholder needs into requirements and constraints. Design converts those requirements into data structures, interfaces, algorithms and testable components. Implementation produces the executable system from that design. Testing compares observed behaviour with expected results, while evaluation asks whether the delivered system actually meets the original requirements. Maintenance responds to defects and changed needs after deployment. A stage is incomplete if its output cannot guide or check the next activity; for example, vague requirements make it impossible to decide whether a test has passed. Projects may revisit stages, but the dependency remains: later decisions need reliable earlier information. Explaining this flow is more useful than memorising an order because it shows why missing or weak outputs create rework.",
  ]),
  explanation("142", "waterfall", "tradeoff", "Why sequential planning helps stable projects but resists late change", [
    "Waterfall development establishes major requirements and plans stages before moving through them in sequence. This can make budgets, responsibilities and approval points easier to define because each stage has expected outputs. It works best when requirements are well understood and unlikely to change, or when formal documentation and sign-off are required. The same structure creates its main weakness: users may not interact with a working system until late, so a misunderstood requirement can survive through design and implementation before it becomes visible. Returning to earlier decisions then affects many completed artefacts and becomes expensive. Waterfall is therefore not automatically outdated or safe. Its suitability depends on whether early certainty and formal control are more valuable than frequent feedback and adaptation.",
  ]),
  explanation("142", "iterative", "process", "Why repeated cycles expose mistakes earlier", [
    "Iterative development builds a version, reviews the result and uses the feedback to improve the next version. A working or testable increment turns assumptions into something stakeholders can inspect, so unclear requirements and usability problems can be discovered before the whole system is complete. Each cycle should have a defined goal and feed evidence back into requirements or design; simply rewriting code repeatedly is not a lifecycle method. Iteration reduces the cost of some changes because less unreviewed work depends on the current decision. It can also create scope drift, repeated rework or architectural inconsistency if priorities and quality controls are weak. The advantage comes from short feedback distance, while the management challenge is keeping those cycles aligned with the overall system and budget.",
  ]),
  explanation("142", "agile", "tradeoff", "Why short feedback cycles help changing requirements", [
    "Agile approaches organise work into short increments and involve users or product representatives frequently. Regular feedback helps the team detect when a feature solves the wrong problem and reprioritise before a large amount of dependent work is built. Small increments can deliver useful value earlier and make progress visible to stakeholders. This flexibility requires active stakeholder participation, disciplined testing and a shared understanding of the overall product. Without them, frequent change can become uncontrolled scope growth or fragmented design. Agile does not mean no planning or documentation; it favours enough planning and documentation to support the next decisions and maintain the system. It is most suitable when learning and change are expected and when stakeholders can provide timely, informed feedback.",
  ]),
  explanation("142", "compare", "comparison", "Why project conditions should choose the lifecycle model", [
    "Lifecycle choice is a trade-off between certainty, feedback, regulation, risk and cost of change. Stable requirements, contractual milestones or safety documentation may favour a more sequential model because decisions and approvals must be traceable before construction continues. Uncertain requirements and user-interface work may favour iterative or agile approaches because prototypes and increments generate information that cannot be obtained from documents alone. Team distribution, stakeholder availability and technical dependencies also matter. A model cannot provide feedback if users are unavailable, and rapid iteration cannot safely ignore a hardware component with a long manufacturing lead time. Strong evaluation therefore names a project condition, explains how the model's feedback and planning mechanism responds to it, and states the resulting benefit or limitation. Calling one model simply faster or better is not a justified choice.",
  ]),
  explanation("142", "artefacts", "mechanism", "Why lifecycle outputs make decisions testable and traceable", [
    "Lifecycle artefacts preserve information that later stages need. A requirements specification defines what success means; design documents show how components and data should satisfy those requirements; a test plan connects inputs and expected results to the behaviour being checked; test records show what actually happened. Traceability links these artefacts so a changed requirement can be followed into affected designs, code and tests. This reduces the chance that one document changes while the implemented system remains inconsistent. Artefacts also support maintenance because a future developer can recover the reasoning behind a decision. Their value depends on accuracy and use: an outdated document can mislead more than no document. The purpose is not paperwork itself but a shared, reviewable record of decisions and evidence.",
  ]),
]);

export const explanationByKey = Object.freeze(Object.fromEntries(
  explanations.map((item) => [`${item.lesson}/${item.targetId}`, item]),
));
