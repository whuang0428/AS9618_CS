import { mechanismVisual } from "./course-v3-mechanism-diagrams.mjs";
import { coreParagraph, coreList, coreTable } from "./course-v3-core-blocks.mjs";
import { s4ids as ids, section4Practice, section4Exams, traceData } from "./course-v3-section4-questions.mjs";
import { section4Programs, programListing } from "./course-v3-section4-programs.mjs";

const table = (title, headers, rows) => ({ type: "table", title, headers, rows, preserveText: true, preserve: true });
const worked = (title, steps) => ({ type: "worked-example", title, steps });
const visual = (title, name, facts, alt) => ({ type: "reviewed-visual", title, asset: `/assets/course-v3/section-4/${name}.svg`, facts, alt, caption: alt, review: "reviewed", preserveText: true });
const unit = (unitKey, heading, objectiveIds, explanation, materials, misconception, prompt, answer) => ({
  unitKey, heading, syllabusId: objectiveIds[0].replace(/\.A\d+$/, ""), objectiveIds, explanation,
  materials: materials.map((m) => ({ ...m, objectiveIds })), misconceptions: [misconception],
  useAuthoredVisual: true, preserveTeachingSteps: true, checkpoint: { prompt, answer },
});
const listing = (key) => programListing(section4Programs[key]);
const workedTrace = traceData("workedTrace", [[20,2,0,21],[21,1,0,22],[22,1,0,23],[23,1,0,21],[21,0,0,22],[22,0,0,23],[23,0,0,24],[24,67,0,25],[25,67,0,26,"C"],[26,67,0,null]]);
export const section4WorkedTrace = workedTrace;

const authored = {
  19: {
    guidingQuestion: "How can the same processor run many different stored programs?",
    diagnostic: { prompt: "Does memory store only the data a program processes?", answer: "No. In the Von Neumann model, instructions and data share the same memory." },
    units: [
      unit("s4-stored-program", "Von Neumann architecture and the stored program concept", ids(1), [
        "A stored program computer holds its instructions in memory. The processor fetches these instructions, decodes them and carries out the specified operations. Loading another instruction sequence changes the task the computer performs.",
        "The basic Von Neumann model uses one memory for instructions and data. The processor contains a control unit, an arithmetic and logic unit and registers. Buses connect the processor to memory and input/output interfaces; instructions and data share transfer paths.",
      ], [visual("Processor and shared memory", "von-neumann", ["The CPU contains the CU, ALU and registers.", "The IAS holds both instructions and data.", "The address bus identifies a location; the data bus transfers values in either direction.", "Control lines carry read/write and other coordinating signals."], "A processor connected to one immediate access store containing instructions and data."), worked("A program and its data in one store", [["Stored items", "Location 100 holds LDD 300; location 101 holds ADD 301; locations 300 and 301 contain 12 and 7."], ["Execution", "The processor fetches the instructions at 100 and 101 and reads their operands from 300 and 301. ACC becomes 19."], ["Changing the task", "Replacing ADD 301 with SUB 301 changes the calculation to 12−7 without changing the processor hardware."]])], "The stored program concept concerns instructions held in memory; it does not mean the whole program is held in the CPU's registers.", "Why is a single memory drawn for two different kinds of contents?", "In this model, instruction words and data values occupy locations in the same addressable store."),
      unit("s4-registers", "General-purpose and special-purpose registers", ids(2, [1,2,3,4,5,6,7,8]), [
        "Registers are small, fast storage locations inside the processor. A general-purpose register can hold operands and intermediate results; a special-purpose register has a defined role. In CPU architecture, ACC has the designated accumulator role and appears with the special-purpose registers. In the syllabus's example assembly model, ACC is also the only register available for general data operations; IX supplies an index offset.",
        "During instruction processing, several registers are needed at once: one identifies the next instruction, one retains the current instruction, and others support memory transfers. Status flags record conditions such as zero, carry, sign or overflow, depending on the processor; they are not the complete arithmetic answer.",
      ], [table("Register roles", ["Register", "What it holds", "Use"], [["PC — Program Counter", "Address of the next instruction", "Select the next instruction fetch"],["MAR — Memory Address Register", "Address being accessed", "Select a memory location for reading/writing"],["MDR — Memory Data Register", "Instruction or data being transferred", "Buffer the value read from or written to memory"],["ACC — Accumulator", "Operand or result", "Hold arithmetic/logic results in the example instruction set"],["IX — Index Register", "Index offset", "Form an indexed effective address"],["CIR — Current Instruction Register", "Current instruction", "Retain the opcode and operand for decoding/execution"],["SR — Status Register", "Condition flags", "Record results/conditions relevant to later operations"]]), worked("Separate address and contents", [["Initial state", "PC=80; Memory[80] contains LDD 500; Memory[500]=23."],["After instruction fetch", "PC=81, MAR=80 and CIR contains LDD 500. MDR has carried that instruction."],["After operand read", "MAR=500 and MDR=23. LDD copies 23 into ACC; CIR can still hold LDD 500."]])], "PC contains an address, not the current instruction. IX supplies an offset; indexed loading obtains the contents at the resulting address.", "Which two registers distinguish the selected memory location from the value transferred?", "MAR holds the selected address; MDR holds the transferred value."),
      unit("s4-components", "ALU, CU, system clock and immediate access store", ids(3), [
        "The ALU carries out arithmetic and logical operations. The CU decodes the instruction held in CIR and generates the signals needed to move values and carry out the selected operation. These are processing and coordination roles respectively.",
        "The system clock generates regular pulses that synchronise processor operations. One instruction can require several cycles. The immediate access store (IAS) is the main memory holding instructions and data available to the processor during execution; it is not the same as long-term secondary storage.",
      ], [table("Components in one calculation", ["Component", "Contribution to adding two stored values"], [["IAS", "Supply the instruction and operand values"],["CU", "Decode the opcode and coordinate transfers"],["ALU", "Add the operands"],["System clock", "Time the coordinated processor operations"]]), worked("Executing ADD 410", [["Given", "ACC=6 and Memory[410]=11; ADD 410 is already in CIR."],["Control and access", "The CU arranges a read of location 410; clocked transfers deliver its value."],["Calculation", "The ALU adds 11 to 6 and the result 17 is retained in ACC."]])], "Clock frequency counts cycles, not necessarily completed instructions.", "Does the IAS perform the addition?", "No. It supplies stored items; the ALU performs the arithmetic."),
    ],
    summary: [["Stored program", "Instructions and data share main memory; the processor follows the stored instruction sequence."],["Registers", "Keep the next address, current instruction, memory-transfer address/value, results, index offset and flags distinct."],["Coordination", "The CU controls operations, the ALU calculates, and the clock synchronises their timing."]],
  },
  20: {
    guidingQuestion: "How do transfer paths, processor design and peripheral interfaces affect a computer's work?",
    diagnostic: { prompt: "Can a memory address and the value at that address be different numbers?", answer: "Yes. Address 420 might contain value 27. The address selects a location; the data is its contents." },
    units: [
      unit("s4-buses", "Address, data and control buses", ids(4), [
        "The address bus carries the address that selects a memory location or an addressed interface. In the basic processor-controlled model it travels outwards from the processor. The data bus carries instructions and data between components and must support reads and writes.",
        "The control bus is a set of coordinating signal lines. Read and write signals travel from the processor; signals such as interrupt requests travel towards it. Calling the control bus bidirectional describes the collection of lines, not a claim that every individual control signal travels both ways.",
      ], [table("A memory transaction", ["Transaction", "Address bus", "Data bus", "Control bus"], [["Read Memory[300]", "Processor → memory: 300", "Memory → processor: stored value", "Read signal towards memory"],["Write 12 at address 301", "Processor → memory: 301", "Processor → memory: 12", "Write signal towards memory"],["Peripheral interrupt request", "Not the event data", "Not the request line", "Interface → processor: request"]]), worked("Read then modify a value", [["Read", "For Memory[300]=8, the processor selects address 300 and issues read; the data bus returns 8."],["Calculate", "An internal addition increases the value to 9."],["Write", "The processor selects address 300 again, places 9 on the data bus and issues write. The stored value becomes 9."]])], "The address bus does not carry the instruction or data value being fetched.", "Why does writing to a location still need an address?", "Memory must know which location should accept the value on the data bus."),
      unit("s4-performance", "Factors affecting processor performance", ids(5, [1,2,3,4,5]), [
        "Processor type matters because instruction sets and internal organisation differ. Designs may use different instruction complexity, pipelining, cache arrangements and amounts of work per clock cycle. A comparison needs a specified workload; a higher clock frequency alone does not establish which different processor is faster.",
        "Additional cores can execute independent tasks or parallel parts of a program concurrently. Sequential dependencies and communication overhead limit the benefit. A wider data bus can transfer more bits at once; a faster clock provides more cycles per second. Neither removes all memory or input/output delays.",
        "Cache stores copies of recently or frequently needed instructions and data close to the processor. A hit avoids a slower main-memory access. Increased cache capacity can help when it retains more of the working set, but performance depends on access patterns and hit rate as well as size.",
      ], [table("Workload and limiting condition", ["Factor", "Potential improvement", "Condition"], [["Processor type", "More suitable operations or work per cycle", "Compare architecture and actual workload"],["Number of cores", "More simultaneous computation", "Enough parallel work exists"],["Data-bus width", "More bits per transfer", "Transfers are a relevant bottleneck"],["Clock speed", "More cycles per second", "Other factors remain comparable"],["Cache memory", "Fewer slow main-memory reads", "Needed items produce useful cache hits"]]), worked("A transfer-bound calculation", [["Given", "A calculation needs 256 bits transferred over a bus once; ignore protocol overhead."],["Compare widths", "At 32 bits per transfer it takes 8 transfers; at 64 bits it takes 4."],["Limit the claim", "This halves the transfer count for these data, not necessarily the total program time, which also includes computation and other waits."]])], "Extra cores, clock speed or cache do not guarantee a fixed proportional speed-up.", "Why might doubling cores leave one dependent calculation nearly unchanged?", "Its next operation may need the previous result, leaving little work for the extra cores to run concurrently."),
      unit("s4-ports", "USB, HDMI and VGA connections", ids(6, [1,2,3,4]), [
        "A port provides a physical and electrical interface for a peripheral connection. The device and computer need compatible signals and protocols. Adapters may convert signals; a connector that fits mechanically is not by itself proof of compatibility.",
        "USB carries digital peripheral data and can provide power. HDMI carries digital video and audio to a compatible display. VGA carries analogue video and does not carry the display's audio. USB capabilities depend on the version, cable and devices, so one speed should not be assigned to every USB connection.",
      ], [table("Port choice", ["Port", "Typical peripheral", "Signal/use"], [["Universal Serial Bus (USB)", "Keyboard, printer, external drive", "Serial digital data; power can also be supplied"],["High Definition Multimedia Interface (HDMI)", "Display or projector with audio", "Digital video and audio"],["Video Graphics Array (VGA)", "Legacy monitor or projector", "Analogue video; separate audio if needed"]]), worked("A presentation station", [["Requirements", "A laptop must receive mouse input and send a presentation with sound to an HDMI display."],["Connections", "Use USB for the compatible mouse and HDMI for the display."],["Legacy alternative", "If only VGA video input is available, a suitable video conversion may be required and sound needs a separate path."]])], "VGA is not a digital audio/video interface.", "Which listed display connection can carry picture and sound together?", "HDMI carries both digital video and digital audio."),
    ],
    summary: [["Buses", "Address selects a destination, data carries the value, and control signals coordinate the transaction."],["Performance", "Explain a mechanism and its workload condition for processor type, cores, bus width, clock and cache."],["Ports", "USB connects peripherals; HDMI carries digital video/audio; VGA carries analogue video."]],
  },
  21: {
    guidingQuestion: "Which values move between registers when an instruction is fetched and executed?",
    diagnostic: { prompt: "Memory[40] contains LDD 600. Is 600 the address of that instruction?", answer: "No. The instruction is at address 40. Its operand refers to data at address 600." },
    units: [
      unit("s4-fetch", "Fetch using register-transfer notation", ids(7, [1,2]), [
        "In this notation, [PC] means the contents of PC and [[MAR]] means the contents of the memory location whose address is in MAR. The arrow points towards the destination. MAR ← [PC] therefore copies the next instruction address into MAR.",
        "The CU issues a memory-read signal; the instruction from the selected location travels to MDR and is copied to CIR. PC advances to the next sequential address. For these examples each instruction occupies one location, so the increment is one. Some fetch operations can overlap; the instruction must reach MDR before it can be copied from MDR to CIR.",
      ], [visual("Instruction fetch: address and value transfers", "fetch-transfers", ["MAR ← [PC] selects the instruction address.", "A read signal causes MDR ← [[MAR]].", "CIR ← [MDR] retains the fetched instruction.", "PC ← [PC] + 1 advances the sequential address independently of the instruction transfer."], "PC supplies MAR; memory returns the instruction through MDR to CIR. PC advances by one."), worked("Fetch from address 200", [["Given", "PC=200; Memory[200] contains LDD 700; instructions occupy one location."],["Address and read", "MAR ← [PC] gives 200. After the read signal, MDR ← [[MAR]] gives LDD 700."],["Retain and advance", "CIR ← [MDR] retains LDD 700. PC ← [PC] + 1 gives 201."]])], "There is no MDR → PC instruction transfer in the standard fetch sequence.", "What does [[MAR]] mean when MAR=700?", "The value stored in memory location 700, not the number 700 itself."),
      unit("s4-execute", "Decode, execute and select the next instruction", ids(7, [1,2]), [
        "Decoding identifies the operation and interprets its operand. The CU then coordinates the transfers and ALU operation required for execution. Execution depends on the instruction: a load reads data, a store writes data, arithmetic calculates a result, and a jump changes the next instruction address.",
        "The PC's sequential increment is not a guarantee that the next instruction will be adjacent. A taken branch can replace PC during execution. At the end of the cycle the processor checks for pending enabled interrupts before continuing the normal instruction sequence.",
      ], [table("Execution after decoding", ["Instruction", "Illustrative register transfers", "Effect"], [["LDD 700", "MAR ← 700; read; MDR ← [[MAR]]; ACC ← [MDR]", "Load contents of location 700"],["ADD 701", "MAR ← 701; read; MDR ← [[MAR]]; ACC ← [ACC] + [MDR]", "Add a memory operand"],["STO 702", "MAR ← 702; MDR ← [ACC]; write; [[MAR]] ← [MDR]", "Store the ACC value"],["JMP 90", "PC ← 90", "Next fetch starts at address 90"]]), worked("Execute the fetched load", [["Given", "CIR contains LDD 700, PC=201 and Memory[700]=45."],["Decode and read operand", "The CU selects load-direct; MAR becomes 700 and a new read transfers 45 to MDR."],["Finish", "ACC ← [MDR] gives 45. PC remains 201 unless an interrupt is accepted before normal execution continues."]])], "Fetching an instruction is separate from fetching a data operand needed to execute it.", "Why can a load instruction require two memory reads in one complete cycle?", "One fetches the instruction; the other reads the data at its operand address."),
    ],
    summary: [["Fetch", "Copy the next address to MAR, read the instruction through MDR to CIR, and advance PC."],["Decode/execute", "The CU identifies the operation and coordinates its required transfers or ALU work."],["Control flow", "A jump can replace the sequential PC; interrupt checking follows the current instruction."]],
  },
  22: {
    guidingQuestion: "How can the processor respond to an event and then resume its interrupted work?",
    diagnostic: { prompt: "Should a printer keep the processor busy until every page is finished?", answer: "No. A completion interrupt can request attention while the processor performs other work between events." },
    units: [
      unit("s4-interrupt-causes", "Purpose, causes and applications of interrupts", ids(8, [1,2,3]), [
        "An interrupt is a request or event requiring the processor to suspend its normal instruction sequence and transfer control to a handler when the request is accepted. This supports response to asynchronous events without continually polling every device.",
        "Requests may originate from hardware, such as a device completing input/output or a timer expiring, or from software requesting a system service. Exceptional conditions such as an arithmetic error also require processor attention. The precise treatment of faults depends on the processor; the basic taught sequence considers interrupt checks at instruction boundaries.",
      ], [table("Event → useful response", ["Cause", "Application", "Handler's work"], [["Keyboard input available", "Interactive input", "Read and buffer the character"],["Disk operation completes", "Asynchronous file input/output", "Record completion and make waiting work ready"],["Timer expires", "Scheduling/timekeeping", "Update time or run the scheduler"],["Power/temperature alarm", "Respond to a hardware problem", "Perform the required protective action"],["Software service request", "Request an OS function", "Enter the relevant service routine"]]), worked("Printer completion", [["While printing", "The processor runs a spreadsheet while the printer outputs a page."],["Event", "The printer interface sets an interrupt request when the page is complete."],["Response", "The relevant routine records completion and may supply the next data; the spreadsheet can later resume."]])], "Interrupts are not only errors and do not necessarily indicate a fault.", "Why is an input-complete interrupt useful when data arrive unpredictably?", "It requests attention when data are ready, avoiding repeated checks that often find nothing."),
      unit("s4-interrupt-handling", "Detection, ISR and restoration of context", ids(8, [4,5,6]), [
        "In the basic fetch-execute model, the processor checks for pending enabled interrupts after the current instruction finishes. Priority and interrupt-enable rules determine which request can be accepted. A lower-priority request can remain pending rather than being lost.",
        "Before transferring control, the processor saves the return address and the state needed to resume the interrupted work. The appropriate Interrupt Service Routine (ISR) services the event and acknowledges or clears the request as required. On return, saved state is restored so normal execution can continue correctly. Registers used by the ISR must be preserved as required by the system's interrupt mechanism.",
      ], [table("Handling sequence", ["Stage", "Required action"], [["Check", "At the end of the current cycle, inspect pending enabled requests"],["Accept", "Apply priority rules and identify the interrupt source"],["Save", "Preserve return PC, status and other required register state"],["Transfer", "Load the relevant ISR address into PC"],["Service", "Handle the event and acknowledge/clear the request"],["Return", "Restore saved state and resume at the saved PC"]]), worked("Resume at the correct point", [["Interrupted state", "An instruction has finished; PC=330, ACC=18, IX=4 and the previous comparison is True."],["During the ISR", "Save the required state before the handler uses ACC and changes condition flags."],["Return", "Restore PC=330, ACC=18, IX=4 and the saved flags. The original program continues at 330 with its own state."]])], "The ISR does not restart the entire interrupted program, and an interrupt request is not necessarily serviced immediately when raised.", "Why preserve status flags as well as PC?", "Later conditional behaviour may depend on the original program's condition results."),
    ],
    summary: [["Event", "Hardware, timing and software events can request processor attention."],["Handling", "Check and accept a request, save state, run the appropriate ISR, then restore and resume."],["Priority", "Pending does not mean accepted; enable and priority rules determine when a request is serviced."]],
  },
  23: {
    guidingQuestion: "How can symbolic assembly instructions become executable machine code?",
    diagnostic: { prompt: "In LDD TOTAL, is TOTAL necessarily the number to load?", answer: "No. TOTAL can be a label for a memory address; LDD loads that location's contents." },
    units: [
      unit("s4-assembly-machine", "Assembly language and machine code", ids(9), [
        "Machine instructions are encoded as binary patterns understood by a particular processor. An instruction contains an opcode and, where required, operand information. Assembly language provides readable mnemonics and symbolic names for the same low-level operations.",
        "An assembler translates assembly source into machine code for its target instruction set. An ordinary mnemonic instruction corresponds to a machine instruction; labels and data declarations organise the source and are not additional operations for the processor to execute. Source for a different instruction set is not automatically interchangeable.",
      ], [table("Three representations", ["Form", "Example", "Meaning"], [["Symbolic assembly", "LDD TOTAL", "Load from the location named TOTAL"],["Resolved assembly", "LDD 6", "Load from memory address 6"],["Machine word", "Opcode bits followed by operand bits", "Processor-specific binary encoding"]]), worked("A supplied encoding", [["Encoding for this example only", "A 16-bit word uses an 8-bit opcode and an 8-bit address. Let LDD be 00000001."],["Resolve the operand", "If TOTAL labels location 6, its 8-bit address is 00000110."],["Encode", "LDD TOTAL becomes 00000001 00000110. These bit assignments are invented teaching data, not a binary encoding prescribed by Cambridge."]])], "Readable mnemonics are not the binary instruction bits the processor executes.", "Can the opcode bits be calculated just from the letters LDD?", "No. The target instruction set's encoding must be supplied or known."),
      unit("s4-two-pass", "Applying both passes of an assembler", ids(10, [1,2]), [
        "During pass one, the assembler scans the source, assigns locations and records labels with their addresses in a symbol table. It advances a location counter according to each instruction or data item's size and can detect errors such as duplicate definitions.",
        "During pass two, the assembler uses the completed symbol table to resolve symbolic operands, translates mnemonics to opcodes and emits machine instructions and data. A forward reference is now resolvable even when its label appeared later in the source; a name that remains undefined is reported as an error.",
      ], [table("Worked symbol table", ["Label", "Assigned address", "Item at that address"], [["START", "0", "LDD FIRST"],["FIRST", "4", "Data 18"],["SECOND", "5", "Data 24"],["TOTAL", "6", "Data 0"]]), worked("Assemble a complete program", [["Source and location rule", "Addresses start at 0; each listed item occupies one location.\n" + listing("assemblerWorked")],["Pass one", "Count all seven items, including the three data declarations. Enter START=0, FIRST=4, SECOND=5 and TOTAL=6."],["Pass two: resolve", "The instructions become LDD 4; ADD 5; STO 6; END. FIRST and SECOND were forward references."],["Supplied teaching encoding", "Use 16-bit words: 8-bit opcode then 8-bit address. LDD=00000001, ADD=00000010, STO=00000011, END=11111111 with operand 0. Data use a full 16-bit unsigned word."],["Pass two: emit", "0  00000001 00000100\n1  00000010 00000101\n2  00000011 00000110\n3  11111111 00000000\n4  00000000 00010010\n5  00000000 00011000\n6  00000000 00000000"],["Check by execution", "The program loads 18, adds 24 and stores 42 at address 6, then ends before the data declarations."]])], "The symbol table contains addresses, not the data values 18, 24 and 0 stored at those addresses.", "Why can pass one not immediately resolve FIRST in the first source line?", "FIRST is defined later; completing the first scan provides its address for pass two."),
    ],
    summary: [["Assembly", "Mnemonics and labels provide a readable form of processor-specific operations."],["Pass one", "Assign locations and build the label-to-address symbol table."],["Pass two", "Resolve labels, encode opcodes/operands and emit instructions and data; report unresolved symbols."]],
  },
  24: {
    guidingQuestion: "Which instruction actually runs next, and which value does its operand supply?",
    diagnostic: { prompt: "With Memory[200]=700 and Memory[700]=12, does LDI 200 load 700 or 12?", answer: "12. Indirect addressing reads the pointer 700 at location 200, then the value at location 700." },
    units: [
      unit("s4-trace-rules", "Trace the executed instruction sequence", ids(11), [
        "Start a trace from the supplied PC, register values, memory contents and input. Fetch the instruction at PC, apply its exact operation, then record the resulting state. Record a row whenever an instruction executes; a loop therefore repeats rows for the same instruction address.",
        "Use the branch condition produced by the preceding compare operation. JPE jumps when that comparison was True; JPN jumps when it was False. A skipped instruction has no trace row. END terminates execution and returns control to the operating system, rather than fetching the following data declarations.",
      ], [table("Trace fields", ["Field", "Record"], [["Executed address", "The address of the instruction just carried out"],["ACC / IX", "Values after the instruction, retaining unchanged values"],["Next PC", "Sequential address or taken branch target"],["Comparison", "True/False from CMP or CMI when needed for a branch"],["Memory/output", "Any stored value or output character"]]), worked("A skipped instruction", [["Given", "At address 10: JMP 12; at 11: LDM #99; at 12: END. Initially ACC=7."],["First row", "Address 10 executes and sets next PC=12; ACC stays 7."],["Last row", "Address 12 returns to the OS. Address 11 has no row, so ACC never becomes 99."]])], "A trace follows actual execution, not simply the printed top-to-bottom source listing.", "Does an unconditional JMP ever have a false condition?", "No. It always transfers to its target when it executes."),
      unit("s4-instruction-groups", "Instruction groups in the example instruction set", ids(12, [1,2,3,4,5]), [
        "Group an instruction by the operation it performs. Movement instructions copy a value, arithmetic instructions calculate a value, and input/output instructions transfer character data between the program and its environment.",
        "Control instructions alter the sequence of execution. JMP does so unconditionally; JPE and JPN depend on a comparison result. CMP and CMI are compare operations: they compare values without themselves jumping. END terminates the program and returns control to the operating system.",
      ], [table("Five syllabus groups", ["Group", "Instructions"], [["Data movement", "LDM, LDD, LDI, LDX, LDR, MOV, STO"],["Input and output of data", "IN, OUT"],["Arithmetic operations", "ADD, SUB, INC, DEC"],["Unconditional and conditional control", "JMP; JPE, JPN"],["Compare operations", "CMP, CMI"]]), worked("Compare followed by control", [["Initial value", "ACC=8; CMP #8 records comparison True without changing ACC."],["Conditional control", "JPE MATCH takes the branch to MATCH because the recorded result is True."],["Different role", "CMP makes the comparison; JPE uses it to choose the next instruction."]])], "Compare operations are a separate group from the conditional jumps that use their results.", "Which group contains STO?", "Data movement: it copies ACC to a memory location."),
      unit("s4-data-movement", "Data movement: ACC, IX and memory contents", ids(13, [1,5]), [
        "An immediate operand supplies a value directly. The example syntax uses # for denary, B for binary and & for hexadecimal: #15, B00001111 and &0F denote the same value. A bare address instead directs an instruction to memory.",
        "Data-movement instructions copy rather than consume their source. LDX adds IX to its address operand and then reads the contents at that effective address. MOV IX copies ACC into IX; it does not copy IX into ACC. STO writes ACC into memory while leaving ACC unchanged.",
      ], [table("Data-movement instruction meanings", ["Instruction", "Effect"], [["LDM #n", "ACC ← n (immediate value)"],["LDD <address>", "ACC ← Memory[address]"],["LDI <address>", "ACC ← Memory[Memory[address]]"],["LDX <address>", "ACC ← Memory[address + IX]"],["LDR #n", "IX ← n (immediate value)"],["MOV IX", "IX ← ACC"],["STO <address>", "Memory[address] ← ACC"]]), worked("Indexed load and store", [["Given", "IX=2, Memory[402]=31; execute LDX 400 then STO 450."],["Resolve and load", "The effective address is 400+2=402. ACC becomes the stored contents, 31."],["Store", "STO 450 puts 31 in Memory[450]; ACC remains 31 and IX remains 2."]])], "LDX loads the contents at address+IX, not the address+IX sum itself.", "If ACC=9, what does MOV IX change?", "IX becomes 9; ACC stays 9."),
      unit("s4-operations", "Arithmetic, comparison, branches and character I/O", ids(13, [2,3,4]), [
        "ADD and SUB combine ACC with an immediate value or the contents of a direct address, retaining the result in ACC. INC and DEC increase or decrease the named register, ACC or IX, by one. When tracing, change only the register named or implied by the instruction.",
        "CMP compares ACC with an immediate value or directly addressed contents; CMI compares ACC with indirectly addressed contents. Neither replaces ACC with the compared value. JPE tests whether the comparison was True (equal); JPN tests whether it was False (not equal). JPN is not a test for a negative number.",
        "IN reads one character and stores its ASCII value in ACC. OUT outputs the character whose ASCII value is in ACC. For example, 65 corresponds to A: OUT with ACC=65 produces A, not the two printed digits 65. END returns control to the operating system.",
      ], [table("Operation forms", ["Instruction", "Action"], [["ADD #n / Bn / &n; ADD <address>", "Add immediate value or addressed contents to ACC"],["SUB #n / Bn / &n; SUB <address>", "Subtract immediate value or addressed contents from ACC"],["INC ACC / INC IX; DEC ACC / DEC IX", "Increase/decrease the named register by one"],["CMP #n / Bn / &n; CMP <address>", "Compare ACC with immediate value or addressed contents"],["CMI <address>", "Compare ACC with Memory[Memory[address]]"],["JMP <address>", "Always set PC to the target"],["JPE <address>; JPN <address>", "Jump if preceding compare was True; jump if False"],["IN; OUT; END", "Read ASCII character; output ASCII character; return to OS"]]), worked("An input-dependent output", [["Given", "Input is E (ASCII 69); ACC and IX start at 0. Each instruction occupies one location.\n0  IN\n1  CMP #69\n2  JPN REJECT\n3  ADD #2\n4  JMP SEND\n5  REJECT: LDM #63\n6  SEND: OUT\n7  END"],["Compare and branch", "IN gives ACC=69. CMP #69 is True, so JPN REJECT is not taken."],["Output", "ADD #2 gives 71; JMP SEND skips the rejection instruction. OUT produces G (ASCII 71), then END finishes the program."]])], "JPE and JPN use the preceding comparison; their letters should not be expanded into invented parity or sign conditions.", "Does CMP #4 change ACC when ACC=6?", "No. ACC remains 6 and the comparison result is False."),
      unit("s4-complete-trace", "Worked program: loop, comparison and output", [...ids(11), ...ids(13, [6])], [
        "The program below repeats DEC and CMP until ACC reaches zero. The same JPN instruction is taken after the first comparison and not taken after the second. The trace therefore includes two visits to each loop instruction.",
        "The load after the loop replaces the loop counter with an ASCII value before OUT executes. Separate the value used for loop control from the later output value; a register's meaning can change during a program.",
      ], [table("Complete execution trace", workedTrace.answerTable.headers, workedTrace.answerTable.rows), worked("Trace the supplied loop", [["Program and initial state", "Initially ACC=0, IX=0, PC=20; Memory[90]=67. Each instruction occupies one address. No input is required.\n" + listing("workedTrace")],["First comparison", "After the first DEC, ACC=1. CMP #0 gives False; JPN returns to LOOP at address 21."],["Second comparison", "After the second DEC, ACC=0. CMP #0 gives True; JPN falls through to 24."],["Completion", "LDD 90 loads 67; OUT prints C; END returns to the OS. IX remains 0 throughout."]])], "A loop trace must include the final failed branch test that lets execution leave the loop.", "How many times does address 23 execute, and how many times is its branch taken?", "It executes twice; the branch is taken only on the first execution."),
      unit("s4-addressing",
      "Immediate, direct, indirect, indexed and relative addressing",
      ids(14, [1,2]),
      [
        coreParagraph("An addressing mode specifies how to obtain an operand or target. Keep the instruction's operand, a selected address and that address's contents separate."),
        coreTable("Rules for resolving an operand", ["Mode", "Resolution rule"], [
          ["Immediate", "Use the operand itself as the value."],
          ["Direct", "Read Memory[address]."],
          ["Indirect", "Read Memory[Memory[address]]: the first read supplies another address."],
          ["Indexed", "Read Memory[base address + IX]."],
          ["Relative", "Target address = stated PC base + signed displacement."]
        ]),
        coreParagraph("For relative addressing, use the PC convention given in the question; it may already include the fetch increment. The example instruction set supplies no separate relative-jump mnemonic, so state the base and displacement explicitly.", "Check the PC base")
      ],
      [mechanismVisual("addressing") , worked("Moving through array elements", [["Given", "An array starts at 800; Memory[800]=21, Memory[801]=34, Memory[802]=55."],["First access", "With IX=1, LDX 800 reads location 801 and sets ACC=34."],["Next access", "INC IX makes IX=2. Another LDX 800 now reads 55 from location 802."]])],
      "A negative relative displacement moves to an earlier address; it is not a negative memory value to load.",
      "What information is missing from 'relative offset −4' alone?",
      "The PC base from which to calculate the effective address."),
    ],
    summary: [["Operand resolution", "Read prefixes and addressing modes before calculating a loaded or compared value."],["Instruction effects", "ACC/IX, memory, comparisons and PC change only as defined by the executed instruction."],["Trace", "Follow taken branches, repeat loop rows, record stores and output characters, and stop at END."]],
  },
  25: {
    guidingQuestion: "How can one instruction change or inspect selected bits without losing the rest of a device value?",
    diagnostic: { prompt: "Are #15, B00001111 and &0F three different numbers?", answer: "No. They denote the same immediate value in denary, binary and hexadecimal." },
    units: [
      unit("s4-shifts",
      "Logical, arithmetic and cyclic shifts",
      ids(15, [1,3,4,13,14]),
      [
        coreParagraph("State the word width, direction and number of positions before shifting. Each panel starts from the same original byte; the panels are not successive instructions."),
        coreList("What fills the vacated position?", [
          ["Logical", "Insert 0 and discard outgoing bits. LSL #n and LSR #n shift ACC logically by n positions."],
          ["Arithmetic", "For two's complement, copy the sign bit when shifting right. A left shift inserts 0, like logical left, but may overflow."],
          ["Cyclic", "Wrap the outgoing bit to the opposite end; the number of 1 bits is unchanged."]
        ]),
        coreParagraph("A left shift multiplies a signed value by a power of two only when the result remains representable. Arithmetic right shift of a negative odd value rounds towards negative infinity: -105 becomes -53.", "Check the numerical result")
      ],
      [mechanismVisual("shifts") , worked("Two logical instructions", [["Given", "ACC is unsigned 8-bit 00110100 (52). Execute LSL #1, then LSR #2."],["Shift left", "LSL #1 gives 01101000 (104); zero enters at the right."],["Shift right", "LSR #2 gives 00011010 (26); two zeros enter at the left."],["Check the scope", "These particular shifts lose only zeros. Other inputs can lose 1 bits, so opposite shifts need not restore the original value."]])],
      "A cyclic result can coincide with an arithmetic or logical result for one input; their rules are still different.",
      "What enters at the left in an arithmetic right shift of a negative value?",
      "Copies of the original sign bit, which is 1."),
      unit("s4-bitwise", "AND, OR and XOR with immediate or memory operands", ids(15, [5,7,8,9,10,11,12]), [
        "Bitwise operations apply independently to corresponding bit positions. AND produces 1 only when both bits are 1. OR produces 1 when at least one bit is 1. XOR produces 1 when exactly one bit is 1, so equal input bits produce 0.",
        "Each of AND, OR and XOR supports immediate #n, Bn or &n forms, or a direct memory-address operand. The operation combines that operand with ACC and stores the result in ACC. For a direct operand, retrieve the contents of the address before applying the bitwise rule.",
      ], [table("Operand forms and bit rules", ["Operation", "Immediate form", "Direct form", "Outputs for input pairs 00, 01, 10, 11"], [["AND", "AND #n / Bn / &n", "AND <address>", "0, 0, 0, 1"],["OR", "OR #n / Bn / &n", "OR <address>", "0, 1, 1, 1"],["XOR", "XOR #n / Bn / &n", "XOR <address>", "0, 1, 1, 0"]]), worked("Same mask, different operations", [["Given", "For each operation independently use ACC=10100110 and mask B00001111 (15). Memory[720]=15 gives the same operand through address 720."],["AND", "10100110 AND 00001111 = 00000110. AND 720 also gives 6."],["OR", "10100110 OR 00001111 = 10101111. OR &0F also gives 175."],["XOR", "10100110 XOR 00001111 = 10101001. XOR #15 also gives 169."]])], "#00001111 is a denary literal, not the binary mask 00001111. Use B00001111 or #15 for that mask.", "Which operation flips precisely the bit positions where a mask contains 1?", "XOR toggles those positions and leaves positions masked by 0 unchanged."),
      unit("s4-device-mask", "Monitor and control a device using masks", ids(15, [2,5,6]), [
        "A device interface can encode several states or controls in one byte. A mask selects the relevant positions: AND with a single 1 tests a bit, while OR with a single 1 sets it. Number bits explicitly; these examples use bit 7 at the left and bit 0 at the right.",
        "A test should operate on a copy of the device value. AND changes ACC by clearing unselected positions, so reload the original value before an unrelated update. To set a control bit while preserving others, OR the original value with the setting mask and write the result back to the device register. AND with a zero at a selected position clears that bit; XOR with a one toggles it.",
      ], [table("Masking actions", ["Goal", "Mask pattern", "Operation"], [["Test bit 4", "00010000", "AND on a copy; zero means clear, non-zero means set"],["Set bit 2", "00000100", "OR, then store the updated value"],["Clear bit 2", "11111011", "AND, then store"],["Toggle bit 2", "00000100", "XOR, then store"]]), worked("Sensor ready and motor enable", [["Given", "The interface value is 00010001. Bit 4 means ready; bit 2 enables the motor."],["Monitor", "AND B00010000 on a copy gives 00010000, so the device is ready."],["Control", "Reload 00010001, apply OR B00000100 and store 00010101 back. The original bits 4 and 0 are preserved."]])], "Testing and setting are different operations; OR alone cannot tell whether a bit was originally clear.", "Does a successful single-bit AND test necessarily produce denary 1?", "No. Testing bit 4 produces 00010000 (16) when set; compare the test result with zero."),
      unit("s4-labels", "Labels for instructions and data", ids(15, [15,16]), [
        "The form <label>: <opcode> <operand> names the address of an instruction. The operand is omitted for an instruction that does not require one, such as END. A branch can use an instruction label as its target.",
        "The form <label>: <data> names a location containing the given data. The label denotes the location's address, not its contents. Loading or storing through the label accesses that location; program execution must end or branch before falling into data declarations.",
      ], [table("Two label forms", ["Source", "Meaning"], [["AGAIN: DEC ACC", "AGAIN names the address of this DEC instruction"],["FLAGS: 36", "FLAGS names a data location initially containing 36"],["LDD FLAGS", "Load the contents at the address named FLAGS"],["STO FLAGS", "Store ACC at the address named FLAGS"]]), worked("Labelled device value", [["Source", "Addresses start at 40; each item occupies one location.\n40  START: LDD FLAGS\n41  OR B00000001\n42  STO FLAGS\n43  END\n44  FLAGS: 36"],["Resolve", "START=40 and FLAGS=44. LDD FLAGS loads 36 (00100100), not 44."],["Update", "OR sets bit 0 and STO writes 37 (00100101) into location 44. END stops before the data item."]])], "A data declaration does not execute as a load instruction.", "What does FLAGS denote in STO FLAGS?", "The destination address associated with FLAGS in the symbol table."),
    ],
    summary: [["Shifts", "Logical inserts zeros, arithmetic right preserves sign, and cyclic wraps outgoing bits."],["Masks", "AND tests or clears, OR sets, and XOR toggles selected positions; preserve the original device value when needed."],["Notation", "Use # for denary, B for binary and & for hexadecimal immediates; bare operands and labels can refer to memory addresses."]],
  },
};

export function authorSection4Lesson(lesson) {
  if (lesson.section !== 4) return lesson;
  const content = authored[lesson.originalLesson];
  if (!content) throw new Error(`Missing authored S4 lesson ${lesson.originalLesson}`);
  return { ...lesson, ...content, summaryMode: "authored", practice: section4Practice[lesson.originalLesson], authoredExamQuestions: section4Exams[lesson.originalLesson] };
}
