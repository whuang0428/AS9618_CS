import { programQuestionData } from "./course-v3-section4-programs.mjs";

export const s4ids = (requirement, numbers = [1]) => numbers.map((n) => `S4.${String(requirement).padStart(2, "0")}.A${String(n).padStart(2, "0")}`);
const q = (id, prompt, objectiveIds, answerPoints, commonError, extra = {}) => ({ id, type: "Application", authored: true, prompt, objectiveIds, answerPoints, marks: answerPoints.length, commonError, ...extra });
const practice = (l, n, ...args) => q(`S4-L0${l}-Q${n}`, ...args);
const exam = (l, n, ...args) => q(`S4-L0${l}-EXAM-${n}`, ...args);
const rows = (values) => values.map(([address, acc, ix, next, output = "—"]) => [String(address), String(acc), String(ix), next === null ? "Return to OS" : String(next), output]);
export const traceData = (key, expectedTrace, finalMemory = {}) => ({
  ...programQuestionData(key), expectedTrace, finalMemory,
  table: { title: "Trace table: state after each executed instruction", headers: ["Executed address", "ACC", "IX", "Next PC", "Output this step"], rows: expectedTrace.map((_, i) => [`Step ${i + 1}`, "", "", "", ""]) },
  answerTable: { title: "Completed trace", headers: ["Executed address", "ACC", "IX", "Next PC", "Output this step"], rows: rows(expectedTrace) },
});
const tracePoints = (data) => data.answerTable.rows.map(([a, acc, ix, pc, out]) => `Address ${a}: ACC=${acc}, IX=${ix}, next PC=${pc}${out === "—" ? "; no output." : `; output character ${out}.`}`);
const traceQuestion = (factory, l, n, key, mapping, expected, memory = {}) => {
  const data = traceData(key, expected, memory);
  const task = {
    dataPractice: "Trace the data-movement sequence below, paying attention to the direction of MOV IX and the pointer used by LDI.",
    arithmeticPractice: "Trace the arithmetic sequence below. Convert each immediate operand to its value and distinguish changes to ACC from changes to IX.",
    branchPractice: "Trace the input-validation program for the supplied character A. Use the CMI result to decide whether the rejection path runs.",
    jumpPractice: "Trace the control-flow example below. Decide which source lines are skipped by JPE and JMP.",
    dataExam: "Complete a trace of the indexed subtraction program. Show how the base address and IX select the data, then record the subtraction and the stored result.",
    loopExam: "Complete the execution trace for the countdown program. Follow every return to AGAIN until the loop finishes and identify the character printed afterwards.",
  }[key];
  return factory(l, n, `${task} ${factory === exam ? "Use the supplied table to record each post-instruction state through termination." : "Complete one row for each executed instruction, including END; record ACC, IX, the next PC and any output character."} ${Object.keys(memory).length ? `State the final contents of ${Object.keys(memory).map((a) => `Memory[${a}]`).join(" and ")}.` : "Show repeated instructions on separate rows."}`, mapping,
    [...tracePoints(data), ...Object.entries(memory).map(([a, v]) => `The final contents of Memory[${a}] are ${v}.`)], "Use memory contents, follow the actual branch, and retain unchanged register values. END returns control to the operating system.", data);
};

export const section4Practice = {
  19: [
    practice(1, 1, "Explain how one main memory containing both instructions and data supports the stored program concept. State how the same processor can carry out a different task.", s4ids(1), ["Instructions and the data they operate on are held in the same memory as binary patterns.", "The processor fetches the stored instructions and executes them.", "Loading a different program changes the sequence of operations without rewiring the processor."], "A program is stored inside the system; it is not a sequence supplied manually for each operation."),
    practice(1, 2, "Identify the register for each role: next instruction address; address of a current memory access; transferred memory value; instruction being decoded; arithmetic result; index offset; condition flags. Compare a general-purpose register with a special-purpose register.", s4ids(2, [1,2,3,4,5,6,7,8]), ["Next instruction address: Program Counter (PC).", "Address of the current access: Memory Address Register (MAR).", "Transferred memory value: Memory Data Register (MDR).", "Instruction being decoded: Current Instruction Register (CIR).", "Arithmetic result: Accumulator (ACC).", "Index offset: Index Register (IX).", "Condition flags: Status Register (SR).", "A general-purpose register can hold operands or intermediate results; a special-purpose register has a designated control or storage role."], "MAR holds an address; MDR holds the value read or written at that address."),
    practice(1, 3, "Describe the roles of the ALU, CU, system clock and IAS when a stored program calculates a total.", s4ids(3), ["The ALU performs the required additions and any logical operations.", "The CU decodes each instruction and issues control signals to coordinate its execution.", "Clock pulses synchronise the processor's operations.", "The IAS holds the program instructions and data currently available for direct access by the processor."], "The clock provides timing; it does not itself calculate the total."),
    practice(1, 4, "Explain why PC=240 and CIR=LDD 600 can be correct at the same time. State which register will hold the loaded value and which register would supply an offset for indexed access.", s4ids(2, [2,5,6,7]), ["The CIR retains the instruction currently being decoded or executed.", "The PC can already point to the next instruction at address 240.", "The value loaded by LDD will be placed in ACC.", "IX supplies the offset if an indexed instruction is used."], "PC and CIR describe different stages; they need not contain related numeric values."),
  ],
  20: [
    practice(2, 1, "Describe a processor read from Memory[420] and a write of 27 to Memory[421]. Identify what travels on each bus and its direction.", s4ids(4), ["For the read, address 420 travels from the processor on the address bus.", "The control bus carries the read signal to memory; the data bus returns Memory[420] to the processor.", "For the write, address 421 travels from the processor on the address bus.", "The data bus carries 27 towards memory and the control bus carries the write signal.", "The data bus supports both transfer directions; control-bus lines include signals to and from the processor."], "Do not place the contents of location 420 on the address bus."),
    practice(2, 2, "Explain why a four-core processor may improve the time to compress four independent files but may give little improvement for a single sequential calculation. Explain why different processor types cannot be ranked using clock speed alone.", s4ids(5, [1,2]), ["Independent compression tasks can run concurrently on separate cores.", "The sequential calculation may have dependencies that prevent useful parallel execution.", "Processor types can differ in instruction set and internal organisation.", "They may perform different amounts of useful work per cycle, so workload and architecture also affect completion time."], "Four cores do not guarantee four times the speed for every program."),
    practice(2, 3, "Calculate the minimum number of data-bus transfers needed for 128 bits using a 32-bit bus and a 64-bit bus. Explain how a higher clock frequency and a larger effective cache can affect execution, with one condition for each claim.", s4ids(5, [3,4,5]), ["A 32-bit bus requires 128/32 = 4 transfers.", "A 64-bit bus requires 128/64 = 2 transfers.", "A higher frequency allows more processor cycles per second, if the architecture and other constraints are comparable.", "A cache hit supplies reused instructions or data faster than main memory, reducing processor waiting.", "A larger cache helps only when it improves useful hits; misses and other bottlenecks still limit performance."], "Bus width is bits per transfer; clock frequency is cycles per second."),
    practice(2, 4, "Suggest a port for each connection and justify your choice: a USB keyboard, a digital display with integrated speakers, and a legacy VGA-only monitor. Compare the signals carried by HDMI and VGA.", s4ids(6, [1,2,3,4]), ["Use USB for the keyboard's peripheral data connection; the connection can also supply power.", "Use HDMI for a compatible digital display and its audio over one connection.", "Use VGA for the specified legacy monitor's analogue video input.", "HDMI carries digital video and audio; VGA carries analogue video and needs a separate audio connection."], "A connector shape alone does not determine every supported USB version or transfer speed."),
  ],
  21: [
    practice(3, 1, "Use register-transfer notation to show the fetch stage when PC=120 and Memory[120] contains LDD 500. Instructions occupy one address. Include the read control signal and state PC and CIR after the fetch.", s4ids(7, [2,1]), ["MAR ← [PC], so MAR becomes 120.", "The CU issues a memory-read signal and MDR ← [[MAR]], loading LDD 500.", "CIR ← [MDR], so CIR contains LDD 500.", "PC ← [PC] + 1, so PC becomes 121; the increment may overlap other fetch transfers."], "Fetch transfers the instruction to CIR, not to PC."),
    practice(3, 2, "Describe the decode and execute stages for CIR=STO 510 and ACC=36. Use register-transfer notation for the memory write and state whether the ACC changes.", s4ids(7, [1,2]), ["The CU decodes STO as a store operation with destination address 510.", "MAR ← 510 and MDR ← [ACC], so MDR becomes 36.", "The CU issues a write signal and [[MAR]] ← [MDR], placing 36 at address 510.", "ACC remains 36; storing copies its contents to memory."], "A memory write uses MDR as the transferred value, not as the destination address."),
    practice(3, 3, "Explain the error in the proposed fetch transfer PC ← [MDR]. Give the correct instruction destination and explain when a different value may legitimately be loaded into PC.", s4ids(7, [1,2]), ["During fetch, MDR contains the instruction read from memory, not normally the next instruction address.", "Use CIR ← [MDR] to retain that instruction for decoding.", "An executed jump may load its target address into PC to change instruction order."], "Do not confuse a branch during execution with the standard fetch transfers."),
  ],
  22: [
    practice(4, 1, "Explain the purpose of interrupts using a printer that signals completion while the processor runs another program. Compare this with repeatedly checking the printer's status.", s4ids(8, [1,2,3]), ["The printer generates a request when its current operation finishes.", "The processor can perform other work between printer events.", "The request causes the relevant event-handling code to run when it is accepted.", "Repeated status checking uses processor time even when the printer has nothing new to report."], "An interrupt request does not mean the printer executes the processor's handler itself."),
    practice(4, 2, "Describe how an enabled interrupt is detected and handled after an instruction finishes. Include the ISR, saved processor state and return to the interrupted program.", s4ids(8, [4,5,6]), ["At the end of the fetch-execute cycle the processor checks for pending, enabled interrupts.", "If a request is accepted, the return address and required processor state are saved.", "The processor obtains the appropriate Interrupt Service Routine (ISR) address and transfers control to it.", "The ISR services the event and acknowledges or clears the request as required.", "The saved state is restored and execution resumes at the saved return address."], "Returning requires restored state; restarting the interrupted program would lose its progress."),
    practice(4, 3, "Identify an interrupt cause for each situation and explain its use: a time slice expires, a temperature alarm is raised, and a program requests an operating-system service.", s4ids(8, [2,3]), ["A timer interrupt lets the operating system regain control for scheduling.", "A hardware interrupt from the alarm interface requests timely handling of the temperature event.", "A software interrupt can request an operating-system service from the running program."], "Hardware events, timing events and software requests are different interrupt sources."),
  ],
  23: [
    practice(5, 1, "Compare assembly language with machine code. Explain why assembly written for one processor instruction set may not run on a processor with a different instruction set.", s4ids(9), ["Assembly uses mnemonic opcodes and may use symbolic addresses.", "Machine code encodes instructions as binary patterns that the processor can execute.", "An assembler translates assembly instructions into the corresponding machine instructions.", "Different instruction sets define different operations, encodings or operand rules, so the same assembly source may need changes and reassembly."], "Assembly language is a low-level language; its mnemonics are not executed directly as text."),
    practice(5, 2, "Complete the pass-one symbol table for the supplied program and give the resolved address operand of each of its first three instructions in pass two. Explain why two passes are useful here.", s4ids(10, [1,2]), ["The symbol table is BEGIN=10, LEFT=14, RIGHT=15, RESULT=16.", "At address 10 the resolved instruction is LDD 14.", "At address 11 the resolved instruction is ADD 15.", "At address 12 the resolved instruction is STO 16.", "The first instructions refer to labels defined later; the completed symbol table lets pass two resolve these forward references."], "A label records the address of its item, not that item's stored data value.", programQuestionData("assemblerPractice")),
    practice(5, 3, "Write the machine words for LDD 14, ADD 15, STO 16 and END using this teaching encoding: each word is 16 bits; the first 8 bits are the opcode and the last 8 bits the unsigned address. LDD=00000001, ADD=00000010, STO=00000011, END=11111111; use an all-zero operand for END. Explain whether these bit patterns are specified by Cambridge's example instruction set.", [...s4ids(9), ...s4ids(10, [2])], ["LDD 14: 00000001 00001110.", "ADD 15: 00000010 00001111.", "STO 16: 00000011 00010000.", "END: 11111111 00000000.", "These opcodes are supplied for this exercise; Cambridge's example instruction list does not prescribe this binary encoding."], "Keep an address operand separate from a stored data value."),
  ],
  24: [
    practice(6, 1, "Identify the instruction group for each set: LDM/LDD/LDI/LDX/LDR/MOV/STO; IN/OUT; ADD/SUB/INC/DEC; JMP/JPE/JPN; CMP/CMI. Distinguish unconditional from conditional control within the fourth set.", s4ids(12, [1,2,3,4,5]), ["LDM, LDD, LDI, LDX, LDR, MOV and STO: data movement.", "IN and OUT: input and output of data.", "ADD, SUB, INC and DEC: arithmetic operations.", "JMP is unconditional control; JPE and JPN are conditional control.", "CMP and CMI: compare operations, which supply the result used by conditional jumps."], "A comparison records a result; it does not by itself change PC to a branch target."),
    traceQuestion(practice, 6, 2, "dataPractice", [...s4ids(13, [1,5,6]), ...s4ids(11)], [[100,0,2,101],[101,9,2,102],[102,9,9,103],[103,12,9,104],[104,12,9,105],[105,12,9,106],[106,12,9,null]], {320:12}),
    traceQuestion(practice, 6, 3, "arithmeticPractice", [...s4ids(13, [2,5,6]), ...s4ids(11)], [[140,10,0,141],[141,15,0,142],[142,12,0,143],[143,16,0,144],[144,10,0,145],[145,11,0,146],[146,10,0,147],[147,10,3,148],[148,10,4,149],[149,10,3,150],[150,10,3,null]]),
    traceQuestion(practice, 6, 4, "branchPractice", [...s4ids(13, [3,4,5,6]), ...s4ids(11)], [[180,65,0,181],[181,65,0,182],[182,65,0,183],[183,66,0,184],[184,66,0,185],[185,66,0,187],[187,66,0,188,"B"],[188,66,0,null]]),
    traceQuestion(practice, 6, 5, "jumpPractice", [...s4ids(11), ...s4ids(13, [3,6])], [[220,5,0,221],[221,5,0,222],[222,5,0,224],[224,6,0,225],[225,6,0,227],[227,6,0,null]]),
    practice(6, 6, "Calculate the loaded operand value for immediate #200, direct 200, indirect 200 and indexed 200 with IX=3, given Memory[200]=250, Memory[250]=18 and Memory[203]=44. Calculate a relative target using a supplied PC base of 90 and displacement −6. Explain the distinction between an effective address and its contents.", s4ids(14, [1,2]), ["Immediate #200 supplies value 200 without a memory lookup.", "Direct 200 loads Memory[200], which is 250.", "Indirect 200 follows address 250 and loads Memory[250], which is 18.", "Indexed 200 uses effective address 203 and loads value 44.", "Relative target = 90 + (−6) = 84.", "An effective address identifies a location; the operand value is the data obtained from that location when the operation reads memory."], "For relative addressing use the stated PC base, not an assumed before-fetch or after-fetch convention."),
  ],
  25: [
    practice(7, 1, "Calculate the six one-place shift results for the 8-bit pattern 10110010: logical left/right, arithmetic left/right, and cyclic left/right. Interpret arithmetic shifts using two's complement and state whether the left arithmetic shift overflows. Discard any bit shifted outside the 8-bit result.", s4ids(15, [1,3,4]), ["Logical left produces 01100100; logical right produces 01011001.", "Arithmetic left produces 01100100, the same bit movement as logical left.", "The original signed value is −78; doubling it gives −156, outside −128 to 127, so the arithmetic left result overflows.", "Arithmetic right produces 11011001, preserving the sign bit; its signed value is −39.", "Cyclic left produces 01100101 by wrapping the old leftmost bit to the right.", "Cyclic right produces 01011001 by wrapping the old rightmost bit to the left."], "Arithmetic right repeats the sign bit; cyclic shifts wrap the outgoing bit. Their results can coincide for a particular input."),
    practice(7, 2, "Calculate the ACC result for each instruction independently, starting each time with ACC=172 and Memory[700]=15: AND #15, AND 700, OR B00001111, OR 700, XOR &0F, XOR 700. Give every result in 8-bit binary and denary.", s4ids(15, [5,7,8,9,10,11,12]), ["AND #15: 00001100 = 12.", "AND 700: 00001100 = 12 because the memory operand is 15.", "OR B00001111: 10101111 = 175.", "OR 700: 10101111 = 175 using the contents of location 700.", "XOR &0F: 10100011 = 163.", "XOR 700: 10100011 = 163 using the same mask from memory."], "Reset ACC before each independent calculation; a bare address refers to stored contents."),
    practice(7, 3, "Explain how to test bit 5 and set bit 1 in an 8-bit device status value without changing unrelated bits in the stored value. Bits are numbered 7 to 0 from left to right. The initial status is 00100100. Give the test result and the updated status, using a fresh copy of the original status for setting.", s4ids(15, [2,5,6,7,11]), ["Use AND B00100000 on a copy to isolate bit 5.", "The test gives 00100000, so bit 5 is set; a zero result would mean it was clear.", "Reload the original status, then use OR B00000010 to set bit 1.", "The updated status is 00100110; store that result back to the device register.", "The AND test changes ACC, so its result must not overwrite unrelated status bits."], "Testing a bit is not the same as clearing every other bit in the device's stored status."),
    practice(7, 4, "Calculate ACC after LSL #2 followed by LSR #3, starting with the unsigned 8-bit value 00101100. Explain what enters each vacated position.", s4ids(15, [1,4,13,14]), ["LSL #2 gives 10110000, which is 176.", "LSR #3 then gives 00010110, which is 22.", "Logical shifts insert zeros; bits leaving the fixed-width value are discarded."], "The second instruction acts on the first instruction's result."),
    practice(7, 5, "Identify the instruction label and data label in the supplied program and give their addresses. Explain the difference between FLAGS and the contents at FLAGS, then state the final stored value in binary and denary.", s4ids(15, [5,11,15,16]), ["START labels the instruction at address 20.", "FLAGS labels the data location at address 24.", "FLAGS resolves to address 24; its initial contents are 129, or 10000001.", "OR B00000010 sets bit 1, producing 10000011 = 131.", "STO FLAGS writes 131 into location 24."], "A data declaration reserves a labelled location; it is not another instruction to execute.", { ...programQuestionData("labelPractice"), finalMemory: {24:131} }),
  ],
};

export const section4Exams = {
  19: [
    exam(1, 1, "Explain why a computer can run a newly installed payroll program using its existing processor and main memory. Refer to the stored program concept and the role of the IAS.", [...s4ids(1), ...s4ids(3)], ["The payroll instructions are loaded into the immediate access store alongside the data needed for processing.", "Both are represented in binary within the common memory system.", "The existing processor fetches the new instruction sequence and performs the operations it specifies.", "Changing this stored sequence changes the task without replacing the processor's physical logic."], "Installing a program changes stored instructions, not the wiring of the ALU."),
    exam(1, 2, "Explain why saving only ACC is insufficient when a processor suspends a program. Refer to PC, IX and the Status Register, giving the information each may need to preserve.", s4ids(2, [2,5,6,8]), ["ACC preserves the current intermediate data result, but does not identify where execution should resume.", "PC provides the return instruction address.", "IX may hold the offset needed to continue accessing the same array element.", "The Status Register contains condition flags that can affect subsequent conditional execution."], "Different registers carry different parts of the program's execution state."),
    exam(1, 3, "Describe how the CU, clock and ALU cooperate to execute an addition after its instruction has reached CIR. Explain why CIR and MDR still have separate roles.", [...s4ids(3), ...s4ids(2, [3,7])], ["The CU interprets the addition opcode held in CIR.", "It issues control signals to select operands and request the ALU addition.", "Clock pulses coordinate the timing of these internal operations.", "The ALU produces the arithmetic result.", "CIR retains the instruction while MDR can hold a separate operand transferred from memory."], "The MDR need not retain the instruction throughout its execution."),
  ],
  20: [
    exam(2, 1, "A processor writes the 16-bit value 0000000000010011 to location 900. Describe the roles of the address, data and control buses. Explain why bidirectional data transfer is still needed even if this particular operation is a write.", s4ids(4), ["The address bus identifies destination location 900.", "The data bus carries the specified 16-bit value from the processor towards memory.", "A memory-write control signal tells memory to accept that value.", "Later reads, including instruction fetches, require data to travel from memory towards the processor."], "Write direction for one transaction does not make the whole data bus unidirectional."),
    exam(2, 2, "Two processors use the same instruction set and clock frequency. P has eight cores and a small cache; Q has four cores and a larger cache. Explain why P may be faster for eight independent jobs but Q may be faster for a single program that repeatedly accesses a working set that fits Q's cache only.", s4ids(5, [1,2,4,5]), ["P can allocate more of the independent jobs to cores at the same time.", "The independent jobs have useful parallel work, so its extra cores can reduce total completion time.", "The single program may not expose enough parallel work to use P's extra cores.", "Q can retain the specified working set in cache and avoid more main-memory waits.", "Equal clock frequency and instruction-set compatibility do not make the two internal cache/core organisations equally fast for every workload."], "Use the given workload to justify a choice; do not declare one processor universally faster."),
    exam(2, 3, "A museum computer must connect a USB barcode scanner, an HDMI display with sound and an old VGA projector. Identify a suitable connection for each. Explain why carrying display data on a 64-bit internal data bus does not imply that every external connector transfers 64 bits in parallel.", [...s4ids(6, [1,2,3,4]), ...s4ids(5, [3])], ["The scanner uses a compatible USB port for peripheral data.", "The display uses HDMI to carry its digital picture and audio.", "The projector uses VGA for analogue video, with a separate audio connection if sound is required.", "Internal data-bus width describes bits carried in one internal transfer.", "External interfaces use their own signalling and protocols; USB and HDMI links do not simply extend that internal parallel bus."], "Keep internal bus width separate from the properties of an external port."),
  ],
  21: [
    exam(3, 1, "Use register-transfer notation to fetch the instruction ADD 710 from address 88. PC initially contains 88 and instructions occupy one location. Explain how Memory[710]=9 is then added to ACC=4 during execution.", s4ids(7, [2,1]), ["MAR ← [PC] selects location 88; a read signal causes MDR ← [[MAR]].", "CIR ← [MDR] retains ADD 710, and PC ← [PC] + 1 gives 89.", "The CU decodes ADD and sets MAR ← 710 for the operand read.", "After the read signal, MDR ← [[MAR]] supplies operand 9.", "ACC ← [ACC] + [MDR] produces 13."], "Fetching ADD 710 and reading its operand require separate memory reads."),
    exam(3, 2, "Describe what happens after JMP 450 has been fetched from location 90. Explain why a trace may show PC=91 during fetch but PC=450 after execution.", s4ids(7, [1,2]), ["Sequential fetch advances the address with PC ← [PC] + 1, giving 91.", "The CU decodes the current instruction as an unconditional jump.", "Execution loads the target with PC ← 450.", "The next fetch uses address 450 rather than 91."], "A PC increment does not prevent a later jump from replacing the next address."),
    exam(3, 3, "Explain why this proposed memory-write notation is wrong: MAR ← [ACC]; [[MDR]] ← 620. Give the correct transfers for storing ACC=58 at address 620 and identify the control signal.", s4ids(7, [2,1]), ["MAR must hold the destination address, so use MAR ← 620.", "MDR must hold the transferred value, so use MDR ← [ACC] to copy 58.", "Use [[MAR]] ← [MDR] to store 58 in the selected location.", "The CU issues a memory-write signal on the control bus."], "An address belongs in MAR and the value being written belongs in MDR."),
  ],
  22: [
    exam(4, 1, "A network interface receives a packet while the processor executes a multiplication. Explain when a pending enabled interrupt is checked in the taught fetch-execute model, why the current instruction is allowed to finish, and how the packet handler is selected.", s4ids(8, [2,3,4,5,6]), ["The processor checks for the pending request at the end of the current instruction's fetch-execute cycle.", "Finishing the multiplication leaves a defined instruction boundary from which execution can later resume.", "The interrupt source or vector identifies the appropriate packet-receive ISR.", "After saving the return state, control transfers to that handler to service the received packet."], "Do not describe the arithmetic instruction as being abandoned halfway through."),
    exam(4, 2, "Explain how a timer interrupt supports sharing a processor between two running programs. Explain why the interrupted program's PC and status flags must be preserved even if the timer ISR performs no arithmetic.", s4ids(8, [1,2,3,4,6]), ["The timer periodically requests operating-system attention after a time interval.", "Its handler lets the scheduler decide whether to resume the same program or run another ready program.", "The saved PC identifies the interrupted program's continuation address.", "Saved flags preserve its earlier condition results; subsequent execution must not inherit another program's flags."], "A timer interrupt provides a scheduling opportunity; it need not force a switch on every occurrence."),
    exam(4, 3, "An enabled high-priority alarm request and an enabled lower-priority disk-completion request are pending together. Describe a suitable handling sequence and explain what must happen to the interrupted context before normal execution continues.", s4ids(8, [4,5,6]), ["At the next interrupt check the processor accepts the higher-priority alarm request first.", "It preserves the interrupted context before running the alarm ISR.", "The disk-completion request remains pending until it can be accepted under the priority and enable rules.", "After the required servicing, the saved registers and return address are restored so the interrupted work can continue correctly."], "Lower priority need not mean discarded; distinguish a pending request from an accepted one."),
  ],
  23: [
    exam(5, 1, "Complete the symbol table after pass one and give the resolved operands used by pass two for the supplied program. Explain whether the data value 25 belongs in the symbol table entry for VALUE.", s4ids(10, [1,2]), ["ENTRY is entered as address 60.", "VALUE is entered as address 63 and COPY as address 64.", "LDD VALUE resolves to LDD 63.", "STO COPY resolves to STO 64.", "The symbol table stores VALUE's address 63; the separate data item at that address contains 25."], "Do not resolve LDD VALUE as LDD 25.", programQuestionData("assemblerExam")),
    exam(5, 2, "Explain how a two-pass assembler handles a jump to a label defined later in the source. Describe what it should report if that label is never defined, and why simply substituting address zero is unsafe.", s4ids(10, [1]), ["Pass one assigns addresses and collects label definitions in a symbol table.", "Pass two looks up the later-defined target using the completed table when generating the jump operand.", "If no definition exists, the assembler reports an undefined-symbol error.", "Substituting zero could silently generate a jump to an unintended instruction rather than the requested target."], "A forward reference can be valid; a missing definition is an error."),
    exam(5, 3, "Write the 16-bit machine word for LDD 63 when a supplied encoding gives LDD opcode 00000110 followed by an 8-bit address. Explain why another processor's assembler might generate a different word for a load operation, and identify the role of the assembly mnemonic.", s4ids(9), ["The unsigned 8-bit form of address 63 is 00111111.", "The resulting word is 00000110 00111111.", "Another instruction set may assign different opcodes, formats or operand rules.", "LDD is a readable mnemonic naming the operation for the assembler to encode."], "Use the encoding supplied in this question rather than the different teaching encoding used earlier."),
  ],
  24: [
    traceQuestion(exam, 6, 1, "dataExam", [...s4ids(13, [1,2,5,6]), ...s4ids(11)], [[40,2,0,41],[41,2,2,42],[42,18,2,43],[43,13,2,44],[44,13,2,45],[45,13,2,null]], {510:13}),
    traceQuestion(exam, 6, 2, "loopExam", [...s4ids(11), ...s4ids(13, [2,3,4,6])], [[60,2,0,61],[61,1,0,62],[62,1,0,63],[63,1,0,61],[61,0,0,62],[62,0,0,63],[63,0,0,64],[64,90,0,65],[65,90,0,66,"Z"],[66,90,0,null]]),
    exam(6, 3, "Calculate the operand values obtained by immediate #100, direct 100, indirect 100 and indexed 100 with IX=4. Memory[100]=108, Memory[108]=19 and Memory[104]=28. Calculate a relative branch target with PC base 300 and displacement −6, and explain why the target is not the contents of location 294.", s4ids(14, [1,2]), ["Immediate #100 supplies the constant 100.", "Direct addressing reads 108 from location 100.", "Indirect addressing first obtains address 108, then reads value 19.", "Indexed addressing forms 100+4=104 and reads value 28.", "The relative branch target is address 294, calculated as 300−6.", "A branch puts the target address into PC; it does not load the data stored at that address as the target."], "Do not confuse an operand read with a control-flow target."),
  ],
  25: [
    exam(7, 1, "A device register initially contains 10010100. Bit 7 means ready and bit 0 controls a lamp; bits are numbered 7 to 0. Explain how to test ready and then switch on the lamp while preserving the other stored bits. Give both masks, the test result and the final register value.", s4ids(15, [2,5,6,7,11]), ["Apply AND B10000000 to a copy of the status to test ready.", "The test result is 10000000, so the ready bit is set.", "Reload the unmodified status before setting the lamp bit.", "Apply OR B00000001 to that status copy.", "Store 10010101 back to the device register; the other seven bits are unchanged."], "Do not write the isolated ready-bit test result back as the full device status."),
    exam(7, 2, "Calculate the ACC value after each of the first four instructions in the supplied program, giving binary and denary. State the final contents of Memory[602] and explain why AND 601 uses 15 rather than 601 as its operand.", s4ids(15, [5,8,9,11]), ["LDD 600 gives 11010110 = 214.", "AND 601 gives 00000110 = 6.", "OR &80 gives 10000110 = 134.", "XOR B00000010 gives 10000100 = 132.", "STO 602 leaves Memory[602]=132; bare address 601 supplies its stored value 15 for AND."], "The prefixes #, B and & specify denary, binary and hexadecimal immediate values respectively.", { ...programQuestionData("bitExam"), expectedAcc: [214,6,134,132,132,132], finalMemory:{602:132} }),
    exam(7, 3, "Calculate the results of logically, arithmetically and cyclically shifting the 8-bit value 01101001 right by one place. Give the results of LSL #1 and an arithmetic left shift of the original value, and explain signed overflow for that left shift.", s4ids(15, [1,3,4,13,14]), ["Logical right gives 00110100 by introducing zero at the left.", "Arithmetic right also gives 00110100 here because the original sign bit is zero.", "Cyclic right gives 10110100 because the old rightmost 1 wraps to the left.", "LSL #1 and arithmetic left both produce 11010010 as an 8-bit pattern.", "The original signed value is 105; 210 cannot be represented in signed 8-bit two's complement, so the arithmetic left shift overflows."], "Same bit movement does not guarantee a valid signed multiplication result."),
  ],
};
